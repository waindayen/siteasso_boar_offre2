---
const { project } = Astro.props;

const donationAmounts = [30, 50, 100, 200];
---

<section id="soutenir" class="py-24 bg-emerald-50">
  <div class="max-w-4xl mx-auto px-4">
    <div class="bg-white rounded-2xl shadow-xl p-8">
      <h2 class="font-display text-3xl font-bold text-gray-900 mb-8 text-center">
        Soutenez ce projet
      </h2>

      <form id="donationForm" class="space-y-8">
        <div>
          <label class="text-lg font-semibold text-gray-900 mb-4 block">
            Choisissez le montant de votre don
          </label>
          <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
            {donationAmounts.map((amount) => (
              <label class="relative">
                <input
                  type="radio"
                  name="amount"
                  value={amount}
                  class="peer sr-only"
                />
                <div class="p-4 text-center border-2 border-gray-200 rounded-lg cursor-pointer transition-all peer-checked:border-emerald-500 peer-checked:bg-emerald-50 hover:border-emerald-300">
                  <div class="text-2xl font-bold text-gray-900">{amount}€</div>
                </div>
              </label>
            ))}
          </div>
          <div class="mt-4">
            <label class="block text-gray-700 mb-2">Ou entrez un autre montant</label>
            <div class="relative">
              <input
                type="number"
                min="1"
                placeholder="Montant personnalisé"
                class="pl-8 pr-4 py-3 w-full border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500"
              />
              <span class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-500">€</span>
            </div>
          </div>
        </div>

        <div class="space-y-4">
          <label class="text-lg font-semibold text-gray-900 block">
            Vos informations
          </label>
          <div class="grid md:grid-cols-2 gap-4">
            <input
              type="text"
              placeholder="Prénom"
              required
              class="px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500"
            />
            <input
              type="text"
              placeholder="Nom"
              required
              class="px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500"
            />
            <input
              type="email"
              placeholder="Email"
              required
              class="px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500"
            />
            <input
              type="tel"
              placeholder="Téléphone"
              class="px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-emerald-500 focus:border-emerald-500"
            />
          </div>
        </div>

        <div class="space-y-4">
          <label class="flex items-start gap-3">
            <input type="checkbox" required class="mt-1" />
            <span class="text-gray-600 text-sm">
              Je souhaite recevoir un reçu fiscal pour déduire 66% de mon don de mes impôts
            </span>
          </label>
        </div>

        <button
          type="submit"
          class="w-full bg-emerald-600 text-white px-8 py-4 rounded-lg text-lg font-medium hover:bg-emerald-700 transition-colors duration-300"
        >
          Faire un don sécurisé
        </button>

        <div class="text-center text-sm text-gray-600">
          <p class="mb-2">🔒 Paiement 100% sécurisé</p>
          <p>Un reçu fiscal vous sera automatiquement envoyé</p>
        </div>
      </form>
    </div>
  </div>
</section>

<script>
document.getElementById('donationForm')?.addEventListener('submit', (e) => {
  e.preventDefault();
  alert('Merci pour votre générosité ! Cette fonctionnalité sera bientôt disponible.');
});
</script>